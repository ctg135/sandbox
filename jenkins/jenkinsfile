pipeline {
    agent any
    options {
        timestamps()
    }
    stages {
        stage('Docker build'){
            steps {
                sh 'ssh 10.99.100.32 "hostname"'
            }
        }
    }
}